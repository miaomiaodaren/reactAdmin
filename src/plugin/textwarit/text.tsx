import * as React from 'react';
import * as PropTypes from 'prop-types';

export default class TextWrite extends React.Component<any, any> {
    static propTypes = {
        times: PropTypes.number
    }
    static defaultProps = {
        times: 300
    }
    render() {
        return (
            <div>
                1212
            </div>
        )
    }
}